namespace Dishes.Webhook.Converters;

//public class DocumentConverter : ValueConverter<Document, string>
//{
//    public DocumentConverter()
//        : base(
//            v => JsonSerializer.Serialize(v, (JsonSerializerOptions)null), // Convert to JSON
//            v => JsonSerializer.Deserialize<Document>(v, (JsonSerializerOptions)null)) // Convert back to Document
//    {
//    }
//}
